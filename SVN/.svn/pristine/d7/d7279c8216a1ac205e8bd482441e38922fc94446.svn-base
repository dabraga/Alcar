#Include 'Protheus.ch'

/**
 * Rotina		:	MA720FIL
 * Autor		:	Ectore Cecato - Totvs Jundiaí
 * Data		:	27/11/2013
 * Descrição	:	Ponto de Entrada para filtrar a data de emissão das OP's para aglutinação
 * Módulo		:  	PCP
**/

User Function MA720FIL()

Local cFiltro := "DToS(C2_EMISSAO) >= '"+ DToS(MV_PAR15) +"' .AND. DToS(C2_EMISSAO) <= '"+ DToS(MV_PAR16) +"' " 

Return cFiltro

